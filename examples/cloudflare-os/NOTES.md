# この本だけのルール（Cloudflare OS 入門 / Essentials）

リポジトリ直下の `AGENTS.md` は **どの本にも効く共通ルール**です。この見本を書いたときに追加で決めた、**この本固有の取り決め**をここに残します。新しい本では、同じ粒度で自分の本用のメモを作ってください。

## 書誌

| 項目 | 内容 |
| --- | --- |
| 書名 | Cloudflare OS 入門 / Cloudflare OS Essentials |
| サブタイトル | 小さなアプリを、安全につなぎながら作る / Build small apps safely with chat, not code |
| 著者 | smilior.com・中川 将広 |
| 読者 | 非エンジニア寄り |
| 構成 | はじめに＋全7章＋おわりに＋付録A/B/C（各版80ページ・ノンブル3〜80） |

## ことば・トーン

| 方針 | 内容 |
| --- | --- |
| 読者 | 非エンジニア寄り。画面操作・たとえ優先。実装詳細は囲みか付録へ |
| 分かりやすい言い換え | 「仕事場」など抽象語は避け、**「場所」「アプリ」「道具」** など具体語を使う |
| はじめに | 一人称（僕）の体験談トーン可（過去の Dify 本に近い語り） |
| 章本文 | 用語の地図と操作の順。はじめにの感想と役割分担する（重複しすぎない） |
| 用語の初出 | 製品名は画面どおり（例: Gatekeepers）。初出で役割を一文で補う |

## 手順・初回フロー

- **初めてインストールする読者**を本線にする（既存アカウント前提で始めない）
- 画面説明は **実際に触る順**（起動 → アカウント作成 → セットアップ → ホーム → 各メニュー）
- 深い設定（API キー詳細など）は、必要な実践章の直前に回す。画面の見方では「画面がある」まででよい
- 実 UI の遷移と食い違わない（例: Create account 成功後はセットアップへ直進）。仕様が変わったら図と本文を同じターンで直す

## 画面キャプチャの撮影環境

- テーマ強制: `localStorage['gadgets:theme-mode'] = 'light'` ＋ `document.documentElement` の `data-mode="light"` / `colorScheme = 'light'`。Playwright なら `colorScheme: 'light'` も併用
- 再撮影スクリプト: `scripts/capture-ch3-light.mjs`（第3章用。他章は同方針で拡張）
- ローカル前提: `http://localhost:8787`（`pnpm run-local`）
- 共通ルール（Light モード・フォーカス切り出し・2x PNG）は `AGENTS.md` と `.claude/skills/kindle-book/references/screenshots.md`

## 制作中に決めたこと

- 第8章「実践④ 自社向けに整える」は削除（`JP/TASKS.md` に ➖ で記録）
- 用語表は章の後半（まとめ直前）に置く。付録Aの用語集と食い違ったら同じターンで両方直す
- 付録Aに本文からの逆引きを追加
