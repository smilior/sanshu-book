# このリポジトリでの作業ルール（Kindle 書籍）

書籍制作は言語版フォルダを作業場とする。日本語版は **`book/JP/`**（`TASKS.md` 正本）、英語版は **`book/EN/`**。

完成原稿の見本は **`examples/cloudflare-os/`**（日英2版・KDP 資料つき）。**見本は編集しない。** 参照するだけにする。その本だけに効いていた取り決めは `examples/cloudflare-os/NOTES.md` にある。

## タスク表を必ず確認する

作業を始める前に、毎回 `book/JP/TASKS.md` を読む。

1. いま **完了済み** の項目と **未着手** の項目を把握する  
2. ユーザーが指定した作業が、タスク表上の **次に進めてよい位置** か判断する  
3. 作業後はタスク表の状態（✅ / 🚧 / 🔲）とメモを更新する  

`book/` がまだ空（新しい本をこれから始める）なら、タスク表は章立ての合意後に作る。骨格 HTML より先にタスク表を立てる。

`kindle-book` スキルのフェーズ（章題 → 骨格 → 肉付け → 仕上げ）と矛盾しないよう、タスク表の並びを優先する。

## 先行作業の禁止と指摘

タスク表が完了していない段階で、**先の章・先の工程の本格追加**をしようとした場合は、実行前に指摘する。

例:

- はじめにが未完なのに第5章だけ全文を書く  
- 骨格 HTML が無いのに奥付の仕上げだけ進める  
- 未着手の章を飛ばして付録だけ厚くする  

対応:

1. **いま未完了の前提タスク**をタスク表から列挙する  
2. 先に進めると手戻りが大きい理由を短く述べる  
3. 推奨する次の一手（タスク表の次の 🔲）を示す  
4. ユーザーが **明示的に「飛ばしてよい／例外で進めて」** と言った場合のみ先行を許可し、タスク表にその旨をメモする  

ユーザーが「第○章を書いて」と指定した場合でも、**前提の章が全部 ✅ である必要はない**（章の肉付けは指定順でよい）。ただし次は指摘する:

- タスク表に **その章が存在しない**  
- **骨格・章立てが未確定**のまま本文を増やそうとしている  
- **章題変更と TASKS / 目次 / 章扉の不一致**を直さずに本文だけ足そうとしている  

## 章題が変わったときの生合成

章題・書名・章の増減・順序変更が合意されたら、**同じターンで**次を揃える。HTML だけ先に直してタスク表を残さない。

| 更新先 | やること |
| --- | --- |
| `book/JP/TASKS.md` | 章名・並び・サマリを新目次に合わせる |
| `book/JP/with-text.html` | 表紙・目次・章扉・柱（running-head）・次章導線 |
| セッションの todo（使う場合） | 章題ラベルを同期 |

章題変更のあとに本文肉付けを続ける場合は、タスク表を更新してから進む。

## 本文の字下げ（必須）

日本語の本文段落は **先頭を1文字分あける（字下げ）**。

- CSS: `.body-prose p { text-indent: 1em; }`（`book/JP/with-text.css` に定義済み。消さない）
- 新規ページ・新規 CSS を足すときも、本文段落には同じ字下げを付ける
- 対象外: 囲み（callout）、手順の注釈、表、キャプション、箇条書き、章扉・表紙など本文以外

## 節見出し（h3）は1ページに1つ（必須）

`h3.sec-title`（節見出し）は **1つの `sheet-wrap` / 1ページあたり最大1つ**。同じページに2つ以上載せない。必ずページ送りする。

- 節が続くときは `sheet-wrap` を分割する（例: `ch1-body-3` と `ch1-body-3b`）
- `h2.page-title`（ページ大見出し）と `h3` の併用は可（キーワード早見・まとめなど）
- 章扉・表紙・目次など `h3.sec-title` を使わないページ型はこのルールの対象外

## 章末まとめの見出し

章末のまとめページ（`page-summary`）の見出しは **「この章のまとめ」** に統一する。  
「この章でできたこと」は使わない。  
まとめの箇条書きは **チェックリスト**（CSS で ☑）。通常の本文リスト（・）とは分ける。

英語版は対応する定型（例: Chapter checklist）に統一し、版のなかで表記を揺らさない。

## ことば・トーン

読者像・一人称・言い換えの方針は **本ごとに決めて、その本のメモに書き残す**（見本: `examples/cloudflare-os/NOTES.md`）。共通で守るのは次。

- 用語の初出は、製品名を画面どおりに書き、役割を一文で補う  
- 抽象語より具体語（「場所」「アプリ」「道具」など）  
- 実装詳細は本文の流れを止めない。囲みか付録へ回す  

## キーワード早見・用語表

- 章の途中に大きな用語表を挟まず、**章の後半〜末尾**（まとめの直前が望ましい）に置く  
- 厳密定義より「たとえ」1行。あとから見返す用と明記してよい  
- 付録の用語集と食い違うときは、同じターンで両方を揃える  

## 図・対比図

| 項目 | ルール |
| --- | --- |
| 置き場 | `book/JP/images/`。HTML からは相対パス（例: `images/os-compare.png`） |
| 形式 | 編集用 SVG ＋ 埋め込み用 PNG（印刷向けに **2x 以上** で書き出し） |
| キャプション | `図: …` 形式。HTML キャプションと図内タイトルの二重掲載は避ける（役割を分ける） |
| 対比図 | 左右（または列）で **1行1軸**。ラベルは中央スパイン化してよい |
| 印刷 | 細いゼブラだけに頼らない。区切り線・帯・枠でコントラストを確保する |
| 差し替え後 | `with-text.html` の `src` が同じならファイル上書きのみ。ブラウザは「更新」で位置を保った再読込 |

図の生成スキル: 技術図は `fireworks-tech-graph` を使ってよい。デザイン判断が難しいときはユーザー了承のうえ Fable（`claude -p --model claude-fable-5`）にレビューを取ってよい。詳細パレットは `.claude/skills/kindle-book/references/diagrams.md`。

### 図解の色（必須）

概念図・対比図・関係図は **色を増やさない**。アクセントはポイント囲みと同じ緑だけにする。

| 用途 | 色 |
| --- | --- |
| アクセント（主役・その図の焦点） | **`#2f9e5f`**（`.callout-point` と同色） |
| 淡い緑の地 | `#f0faf4` / `#f7fcf9`（主役側の行・カードの薄い背景） |
| 既存・副次・ふつう側 | グレー `#6e6a64` / 枠 `#c8c4bc` / 線 `#8a8578` / `#d8d4cc` |
| 紙背景 | `#FFFEFA`（書籍地色） |
| 本文・タイトル | 墨 `#3A3630` / 補足 `#5a5550`・`#8A8578` |

**やってよい:** 主役カラム／重要ノードだけ緑ヘッダー・緑枠・緑の矢印。注記を緑の点線枠（ポイント囲みに近い）にする。  
**やらない:** オレンジ・ブルー・複数の強い役割色の併用。虹色のノード分け。製品 UI スクショの再着色（スクショは実画面のまま）。

見本: `examples/cloudflare-os/JP/images/os-compare.svg` / `ch1-roles.svg` / `ch1-env-share.svg`。新規図はこのトーンに合わせる。

### 図の作成後チェック（必須）

SVG/PNG を書き出したあと、**ユーザーに渡す前に** 次を必ず確認する。問題があれば直してから報告する。

1. **文字の重なり** — タイトル・ノード名・説明・矢印ラベル・注記が互いに重なっていない  
2. **線と文字** — 矢印・枠線が文字の上を通っていない（ラベルは隙間に置き、必要なら背景ピルを付ける）  
3. **はみ出し** — テキストや図形が viewBox / カード枠の外に切れていない  
4. **余白** — ノード同士・ラベルとノードのあいだに十分な隙間がある  
5. **コントラスト** — 文字色が背景に対して薄い（特にグレー地・色帯の上）  
6. **文言** — 誤字・途中切れ・重複ラベルがない  
7. **目視** — 書き出した **PNG を画像として開き**（またはプレビューし）目視確認する。SVG の座標計算だけで「大丈夫」としない  
8. **色トーン** — 緑＋グレー基調になっているか。オレンジ／多色になっていないか  

チェックで直した場合は、同じパスの SVG と PNG を両方更新する。

## 画面キャプチャ（UI スクショ）（必須）

製品画面の差し込みは、**Light モード**と**フォーカス切り出し**を原則とする。全面ダークや余白だらけのフル画面は、B5 図枠では文字が小さくなり読めない。

| 項目 | ルール |
| --- | --- |
| テーマ | **Light モード**で撮る。ダークのまま載せない（ユーザーがダーク指定したときだけ例外） |
| 切り出し | **説明に必要な領域だけ**をトリミングする。フォームはカード周り、アプリ画面は左ナビ＋主領域など。巨大な上下余白は落とす |
| 解像度 | **deviceScaleFactor 2** 以上の PNG |
| 置き場 | `book/JP/images/`（例: `ch3-signin.png`）。HTML は `figure-box-img` ＋必要なら `.figure-box-entry` |
| 目視 | 差し込み前に PNG を開き、**文字が読めるか・端が切れていないか・Light か**を確認する |
| 本文との一致 | 実 UI の遷移と食い違わない。仕様が変わったら図と本文を同じターンで直す |

撮影環境（テーマ強制の方法・ローカル URL・再撮影スクリプト）は本ごとに違う。**その本のメモに書き残す**（見本: `examples/cloudflare-os/NOTES.md` と `examples/cloudflare-os/scripts/capture-ch3-light.mjs`）。スキル側の詳細は `.claude/skills/kindle-book/references/screenshots.md`。

## 手順・初回フローの書き方

- **初めて使う読者**を本線にする（既存アカウント前提で始めない）  
- 画面説明は **実際に触る順**（起動 → アカウント作成 → セットアップ → ホーム → 各メニュー）  
- 深い設定（API キー詳細など）は、必要な実践章の直前に回してよい。画面の見方では「画面がある」まででよい  
- OS 別手順（Mac / Win）は **同じページに混ぜない**。分岐したらページを分ける

## 執筆注記・脚注

- 執筆時点・Early Access などの注記は本文の流れを止めないよう、**ページ下部**（例: `.preface-footnote`）に置く  
- 本文段落の末尾に長い ※ 注を直書きしない  
- **「続きは次のページです」など、紙面めくりを促す文言は書かない。** 書籍ではページが続くのが前提。手順がページをまたぐときは、文を自然に切って次ページから続ければよい（`p.next-hint` の章末導線は別）  

## ポイント囲み（ラフ・行動喚起）

読者に「次は手を動かす」などを促す短い注釈は、専用クラス **`.callout-point`** を使う。

| 項目 | 内容 |
| --- | --- |
| 見た目 | 緑の点線枠・白背景・ややいびつな角丸。左側に指の線画アイコン |
| 見出し | キッカーは「ポイント」（`.callout-point-kicker`） |
| 用途 | 概要のあとで実践へつなぐ、励まし・行動喚起。長い解説は本文や通常の callout へ |
| 字下げ | 囲み内の段落は **字下げしない**（`text-indent: 0`） |
| 実装 | HTML は見本の第1章まとめ（`examples/cloudflare-os/JP/with-text.html`）をコピー。スタイルは `with-text.css` の `.callout-point*`。消さない |

通常の囲み（`.callout-text` ＋ icon-a/b/c）とは別物。注意・用語の定番囲みには従来の callout を使う。

## プレビュー用ツールバー

`with-text.html` のツールバー（`no-print`）:

- **更新**: スクロール位置を保ったまま再読み込み（編集プレビュー用。消さない）  
- **印刷 / PDF**: 印刷ダイアログ  

新規ページを足しても、この2ボタンと位置復元スクリプトは維持する。

## 成果物の場所

```
book/                   # ここで本を作る（作業場）
├── JP/                 # 日本語版: TASKS.md / with-text.html / images/
├── EN/                 # 英語版（必要なら）
└── docs/JP|EN/         # KDP・表紙依頼
examples/               # 完成原稿の見本（編集しない）
└── cloudflare-os/
    ├── NOTES.md        # その本だけのルール
    ├── JP/ EN/         # 本文
    ├── docs/JP|EN/     # KDP・表紙依頼
    └── scripts/        # キャプチャ再撮影スクリプト
docs/                   # テンプレの使い方（GitHub Pages）
```

詳細な制作手順は `.claude/skills/kindle-book/SKILL.md` を参照する。
